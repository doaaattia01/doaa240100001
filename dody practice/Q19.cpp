#include <iostream>

void solve() {
    int N;
    if (!(std::cin >> N)) {
        return;
    }

    
    std::cout << 0 << std::endl;
}

int main() {
    std::ios_base::sync_with_stdio(false);
    std::cin.tie(NULL);

    solve();

    return 0;
}
